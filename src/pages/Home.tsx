import React from "react";
import NavBar from "../components/NavBar";

const Home = () => {
  return (
    <div className="flex flex-col h-screen">
      <NavBar />
    </div>
  );
};

export default Home;
